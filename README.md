# AFTERIMAGE

Experimental fashion studio combining art direction, asymmetry, scroll-led storytelling, expressive type, and usable unconventional structure.

Static HTML/CSS; run with `python3 -m http.server 4173`. Cloudflare Pages-compatible, not deployed.